#include<iostream>
using namespace std;
int main(){
    int i = 1, NumFatorial = (cin >> (cout  << "Digite o numero a ser feito o fatorial:\n", NumFatorial), NumFatorial);
    for (NumFatorial; NumFatorial > 1; NumFatorial--) {cout << (i *= NumFatorial) << "\n";};
    return 0;
}